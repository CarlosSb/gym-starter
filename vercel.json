{
    "buildCommand": "npm run build",
    "installCommand": "npm install",
    "framework": "nextjs",
    "functions": {
        "app/api/**/*.ts": {
            "maxDuration": 10
        }
    },
    "regions": [
        "gru1"
    ],
    "env": {
        "NODE_ENV": "production"
    }
}